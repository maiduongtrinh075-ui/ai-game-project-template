# Add Feature

When adding a new feature:

1. Confirm which existing module should own it.
2. Avoid introducing broad shared abstractions too early.
3. Keep the change local where possible.
4. Update the relevant system doc if the feature changes module behavior.
