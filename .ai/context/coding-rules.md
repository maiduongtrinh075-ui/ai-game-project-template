# Coding Rules

- Keep modules small and responsibility-driven.
- Avoid hidden coupling across systems.
- Separate state, rules, and presentation when practical.
- Prefer descriptive names over abbreviations.
- Add comments only when the behavior is not obvious from code.
- Preserve backward-compatible data contracts unless the task explicitly allows schema changes.
