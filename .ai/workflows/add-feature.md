# Add Feature Workflow

1. Confirm which existing module owns the feature.
2. Avoid creating a new shared abstraction too early.
3. Add data definitions only as needed.
4. Keep the external contract stable where possible.
5. Update docs if the feature changes module behavior.
